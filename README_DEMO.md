# PYP Demo — Quick Start

This README describes the minimal steps to run the day-1 demo: one module + one scenario, full debrief, certificate placeholder, and verification.

## Dependencies (install)
```bash
npm install recharts
# If running server-side PDF creation in future, install:
# npm install pdfkit qrcode
