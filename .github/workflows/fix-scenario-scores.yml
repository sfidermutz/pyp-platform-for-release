name: Fix scenario scores (add defaults & validate)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Ensure scripts exist
        run: |
          ls -la scripts || true

      - name: Run metadata fixer (moduleId, metrics.core)
        run: |
          if [ ! -f scripts/fix-scenario-metadata.js ]; then
            echo "scripts/fix-scenario-metadata.js missing. Please add it."
            exit 2
          fi
          node scripts/fix-scenario-metadata.js

      - name: Run add-default-scores script
        run: |
          if [ ! -f scripts/add-default-scores.js ]; then
            echo "scripts/add-default-scores.js not found; please add it."
            exit 2
          fi
          node scripts/add-default-scores.js

      - name: Run strict validator
        run: |
          if [ ! -f scripts/validate-scenarios-strict.js ]; then
            echo "validator scripts/validate-scenarios-strict.js not found; cannot validate."
            exit 2
          fi
          node scripts/validate-scenarios-strict.js

      - name: Commit & push changes (if any)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.email "actions@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git add data/scenarios/*.json || true
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "chore(scenarios): add placeholder metadata + score & ideal_confidence defaults for strict validation"
          git push "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git" HEAD:main
